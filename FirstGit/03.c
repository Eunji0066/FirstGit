////3. count 변수를 100번 출력
//
//#include <stdio.h>
//#define MAX 100
//
//void generateNumbers(); // 숫자를 100번 세서 배열에 저장하는 함수 (로직 분리)
//void printNumbers(); //배열에 담긴 내용을 출력만 하는 함수(표현 분리)
//
//int numbers[MAX];
//
//int main() 
//{
//	generateNumbers();
//	printNumbers();
//	return 0;
//}
//
//void generateNumbers()//0~99
//{
//	for (int count = 0; count < MAX; count++) 
//	{
//		numbers[count] = count;
//	}
//}
//
//void printNumbers()//1~100
//{
//	for (int count = 0; count < MAX; count++)
//	{
//		printf("[%d]Hello World!\n", numbers[count] + 1);
//	}
//}
//
//
////------------------------------------------------------------------------------
//
///*
//#include <stdio.h>
//
//int main()
//{
//	for (int count = 0; count < 100; count++) 
//		printf("[%d]Hello, World!\n", count);
//}
//
//ai질문
//
//1: #define MAX 100 > MAX라는 숫자 = 100으로 정의한 거 ? True = 1, False = 0처럼 ?
//2 : int numbers[MAX]; > int numbers까진 자료형 / 함수명인거 알겠는데, [MAX]는 뭐임 ? 왜 대괄호를 쓰는 거임 ?
//3 : void generateNumbers(), void printNumbers() 이건 선언과 정의를 동시에 한 거지 ?
//4 : numbers[i] = i > 이건 무슨 문법인지 모르겠다.왜 대괄호 ? numbers[i]는 i라고 ? 이거 생략하고 바로 아래 void printNumbers()에 printf(생략), i);로 쓰면 안 돼 ?
//
//ai답변
//
//1. #define MAX 100 의 의미
//맞아요! 정확히는 '치환(Replace)' 명령이에요.
//컴파일러가 코드를 읽기 전에, 소스 코드에 있는 모든 MAX라는 글자를 100으로 바꿔치기합니다. True = 1처럼 의미를 고정하는 것과 비슷하죠.
//
//왜 써요? 나중에 100번이 아니라 500번으로 바꾸고 싶을 때, 코드 전체를 뒤지지 않고 맨 위 상수의 숫자만 딱 바꾸면 되니까 유지보수가 엄청 편해져요.
//
//2. int numbers[MAX]; 와 대괄호 []
//이것을 **배열(Array)**이라고 불러요.
//
//int a; 가 정수 한 개를 담는 상자라면,
//
//int numbers[100]; 은 정수 100개가 일렬로 붙어 있는 기차라고 생각하면 돼요.
//
//왜 대괄호를 써요? "이 변수는 단독 주택이 아니라, [100]개의 방이 있는 아파트야!"라고 컴퓨터에게 알려주는 약속된 문법이에요.
//
//3. void 함수들의 형태
//네, 맞습니다!
//보통 큰 프로젝트에서는 헤더(.h)에 선언만 하고 소스(.c)에 구현을 따로 하지만, 지금처럼 연습할 때는 함수 머리(이름)와 몸통(내용)을 한 번에 써서 선언과 정의를 동시에 처리하는 경우가 많아요.
//
//4. numbers[i] = i; 문법과 생략 가능 여부
//이 문법의 의미
//배열의 i번째 방에 숫자 i를 집어넣으라는 뜻이에요.
//
//i가 0일 때: numbers[0] = 0; (0번 방에 0 저장)
//
//i가 1일 때: numbers[1] = 1; (1번 방에 1 저장)
//이렇게 기차 칸마다 번호표를 붙여서 저장하는 과정이에요.
//
//"생략하고 바로 출력하면 안 돼?"
//기술적으로는 당연히 되죠! 은지가 처음에 짰던 코드가 바로 그 방식이에요. 하지만 교수님이 "세는 곳과 출력하는 곳을 나누라"고 하신 미션을 수행하려면 이 과정이 꼭 필요해요.
//
//나누지 않았을 때: 숫자를 세자마자 바로 외쳐버림 (데이터가 휘발됨).
//
//나눴을 때: 숫자를 일단 수첩(배열)에 다 적어놓고(generate), 나중에 필요할 때 수첩을 보고 읽음(print).
//
//
//*/